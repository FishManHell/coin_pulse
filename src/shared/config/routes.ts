export const ROUTES = {
  dashboard: "/dashboard",
  login: "/login",
  register: "/register",
  profile: "/profile",
  settings: "/settings",
  watchlist: "/watchlist",
  portfolio: "/portfolio",
} as const;
